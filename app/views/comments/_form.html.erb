<div id="modal-overlay"></div>
<div id="modal-content">
	<div>
		<%= form_for(@comment) do |f| %>
			<%= f.text_field :content %>
			<% if current_user.trainer == true %>
				<%= f.collection_select :receiver_id, User.customers, :id, :name %>
				<%= f.submit '投稿' %>
			<% else %>
				<%= f.collection_select :receiver_id, User.trainers, :id, :name %>
				<%= f.submit '投稿' %>
			<% end %>
		<% end %>
		<p>「閉じる」か「背景」をクリックするとモーダルウィンドウを終了します。</p>
	</div>
</div>
<style type="text/css">
	#modal-content{
	width:50%;
	margin:1.5em auto 0;
	padding:10px 20px;
	border:2px solid #aaa;
	background:#fff;
	z-index:2;
	position: fixed;
}
#modal-overlay{
	z-index:1;
	display:none;
	position:fixed;
	top:0;
	left:0;
	width:100%;
	height:120%;
	background-color:rgba(0,0,0,0.75);
}
</style>