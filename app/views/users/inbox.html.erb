<div class="user-inbox-menu clearfix">
<div class="user-inbox-sidemenu">
	<% @during_a_call.each do |sender| %>
	<div>
		<%= link_to sender.name, inbox_show_user_path(sender.id),remote: true,class: 'user-inbox-sidemenu-user' %>
	</div>
	<% end %>
	<%= link_to 'コメント' , new_comment_path, remote:true %>
</div>
<div id="talk-room"></div>
<div id="comment-form"></div>